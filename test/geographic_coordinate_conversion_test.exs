defmodule GeographicCoordinateConversionTest do
  use ExUnit.Case
  doctest GeographicCoordinateConversion

  test "greets the world" do
    assert GeographicCoordinateConversion.hello() == :world
  end
end
